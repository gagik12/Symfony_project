CREATE TABLE user (id BIGINT AUTO_INCREMENT, login VARCHAR(50) NOT NULL UNIQUE, password VARCHAR(100) NOT NULL, name VARCHAR(40) NOT NULL, last_name VARCHAR(50) NOT NULL, role VARCHAR(10) NOT NULL, created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, PRIMARY KEY(id)) ENGINE = INNODB;
